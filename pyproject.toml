# pyproject.toml

# Note: Many fields are autogenerated by 'WIPACrepo/wipac-dev-py-setup-action'.
#       See this repo's GitHub Actions workflow(s) (.github/workflows/*.yaml)
#       and https://github.com/WIPACrepo/wipac-dev-py-setup-action for details.

[build-system] # do not edit — autogenerated by wipac-dev-py-setup-action
requires = ["setuptools>=78.1", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "scitoken-issuer" # do not edit — autogenerated by wipac-dev-py-setup-action
description = "WIPAC - SciToken Issuer"
readme = "README.md"
authors = [{name = "WIPAC Developers", email = "developers@icecube.wisc.edu"}]
keywords = [
    'transfer',
]
license = {text = "MIT License"}
classifiers = [
    'Development Status :: 3 - Alpha',
    'Environment :: Console',
    'License :: OSI Approved :: MIT License',
    'Operating System :: OS Independent',
    'Programming Language :: Python :: 3',
    'Topic :: Software Development :: Libraries :: Python Modules',
]
requires-python = ">=3.13, <3.15" # do not edit — autogenerated by wipac-dev-py-setup-action
dependencies = [
    'motor',
    'prometheus_client',
    'pymongo',
    'tornado',
    'wipac-dev-tools',
    'wipac-keycloak-rest-services>=1.4.123',
    'wipac-rest-tools>=1.11',
]
dynamic = ["version"] # do not edit — autogenerated by wipac-dev-py-setup-action

[project.optional-dependencies]
tests = [
    'bs4',
    'pytest',
    'pytest_asyncio',
]
mypy = [
    'bs4',
    'pytest',
    'pytest_asyncio',
] # do not edit — autogenerated by wipac-dev-py-setup-action

[project.urls]
repository = "https://github.com/WIPACrepo/scitoken-issuer"

[tool.pytest.ini_options]
asyncio_mode = "auto"

[tool.setuptools]
include-package-data = true
license-files = ["LICENSE"]
packages = [
    'scitoken_issuer',
] # do not edit — autogenerated by wipac-dev-py-setup-action

[tool.setuptools.package-data]
"*" = ["py.typed"] # do not edit — autogenerated by wipac-dev-py-setup-action

[tool.setuptools.package-dir]
"" = "src"

[tool.setuptools_scm]
write_to = "src/scitoken_issuer/version.py"
fallback_version = "CANNOT_BUILD_WITHOUT_GIT_DIR" # used when SCM metadata is missing (no '.git/') — if '.git/' exists but does not contain tags, a commit-based version (like '0.1.dev1+g79c9b4b') will be used instead.

[tool.ruff]
exclude = ["env", "*tests", "bin", "docs", "resources"]
target-version = "py313"

[tool.ruff.lint]
ignore = ["E203", "E226", "E228", "E231", "E501"]
